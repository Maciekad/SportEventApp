import User from "../model/user";

const authResolver = {
    Mutation: {
        registerUser(root: any, user: User) {
            try {
                
            } catch (error) {
                
            }
        }
    },
};
export default authResolver;